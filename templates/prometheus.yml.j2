# Prometheus configuration - generated by Netra
global:
  scrape_interval: {{ scrape_interval }}

scrape_configs:
  - job_name: node
    static_configs:
      - targets:
{% for target in scrape_targets %}
        - "{{ target }}"
{% endfor %}
